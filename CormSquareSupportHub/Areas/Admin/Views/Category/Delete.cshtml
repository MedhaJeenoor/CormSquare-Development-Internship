@*@model Category

@{
    ViewData["Title"] = "Delete Category";
}

<div class="card shadow border-0 mt-4">
    <div class="card-header bg-danger bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="py-2 text-white">Delete Category</h2>
            </div>
        </div>
    </div>

    <div class="container mb-5 pb-5">
        <div class="card-body p-4">
            @* <h4 class="text-center text-danger">Are you sure you want to delete this category?</h4>
            <hr />

            <div class="mb-3 row p-1">
                <label class="fw-bold">Category Name:</label>
                <input class="form-control" value="@Model.Name" readonly />
            </div>

            <div class="mb-3 row p-1">
                <label class="fw-bold">Description:</label>
                <textarea class="form-control" readonly>@Model.Description</textarea>
            </div>

            <div class="mb-3 row p-1">
                <label class="fw-bold">Parent Category:</label>
                <input class="form-control" value="@(Model.ParentCategory?.Name ?? "None (Parent Category)")" readonly />
            </div>

            <!-- Template Section -->
            <div class="mb-3 p-1">
                <label class="fw-bold">Template:</label>
                <div class="border p-3 bg-light">
                    @Html.Raw(Model.HtmlContent) <!-- Display saved template -->
                </div>
            </div>

            <!-- Attachments Section -->
            @if (Model.Attachments != null && Model.Attachments.Any())
            {
                <div class="mb-3">
            @*         <label class="fw-bold">Attachments:</label> *@
@*         <ul class="list-group mt-2"> *@
@*             @foreach (var attachment in Model.Attachments) *@
@*             { *@
@*                 <li class="list-group-item"> *@
@*                     <a href='' target="_blank">@attachment.FileName</a> *@
@*                 </li> *@
@*             } *@
@*         </ul> *@
@*     </div> *@
@* }

            <!-- References Section -->
            @* @if (Model.References != null && Model.References.Any()) *@
@* { *@
@*     <div class="mb-3"> *@
@*         <label class="fw-bold">References:</label> *@
@*         <ul class="list-group mt-2"> *@
@*             @foreach (var reference in Model.References) *@
@*             { *@
@*                 <li class="list-group-item"> *@
@*                     <a href='' target="_blank">@reference.Url</a> *@
@*                 </li> *@
@*             } *@
@*         </ul>
            @*     </div>
             }

            <!-- Buttons -->
            <div class="text-center mt-4">
                <form method="post" asp-action="DeleteConfirmed">
                    <input type="hidden" asp-for="Id" />
                    <button type="submit" class="btn btn-danger mx-2" style="width:200px">
                        <i class="bi bi-trash"></i> Delete
                    </button>
                    <a asp-controller="Category" asp-action="Index" class="btn btn-outline-secondary mx-2" style="width:200px">
                        <i class="bi bi-arrow-left-circle"></i> Cancel
                    </a>
                </form>
            </div>
        </div>
    </div>
</div>

<div class="mb-5 pb-5"></div>
*@